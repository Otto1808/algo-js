let numbers = [100, 101, 102];

let sum = 0;

numbers.forEach( num => {
    sum += num;
  })

let avg = sum / numbers.length;
console.log(avg);

/* let numbers = [1, 2, 3, 4, 5];

let sum = 0;

numbers.forEach( num => {
    sum += num;
  })

let avg = sum / numbers.length;
console.log(avg); */